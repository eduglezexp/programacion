package ies.puerto;

public class Ejercicio1 {

    /**
     * Función que realiza el cálculo del área en triángulo.
     * @param base del triangulo
     * @param altura del triangulo
     * @return resultado de la operacion base por altura dividido entre dos
     */
    public int areaTriangulo(int base, int altura) {
        int resultado = 0;
        resultado = (base * altura)/2;
        return resultado;
    }
}